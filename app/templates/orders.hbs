<div class="container">
	<div class="row">
		{{outlet}}

		<p>This is orders page</p>
		{{#each model as |order|}}
		{{#link-to 'orders.order' order}}
		<p>Order {{order.id}} for {{order.name}}</p>
		{{/link-to}}
		{{/each}}
	</div>
</div>