<div class="container">
	<div class="container">
		{{outlet}}
	</div>
	<div class="container orders-container">
		<h4>Recent Orders</h4>
		{{#each model as |order|}}
		{{#link-to 'orders.order' order}}
		<p>Order {{order.id}} for {{order.name}}</p>
		{{/link-to}}
		{{/each}}
	</div>
</div>